---
description: Workflow A — 直接從 SPEC 產生 Verilog RTL（中文）
globs: workflow-a-direct/**/*
alwaysApply: false
---

# Workflow A：直接 SPEC → RTL

## 觸發

執行 Workflow A；建議 @rtl-workflow-a（skill），或 @workflow-a.zh（本 rule）。

## SPEC 讀取順序

1. `spec/design.spec.md`
2. `spec/design.spec.txt`
3. `spec/` 內**唯一** `.md` / `.txt`（排除 `*.example`）
4. 找不到則回報並停止

支援 `.txt` / `.md`；SPEC 可為中文或英文。

## 限制

- **不得**修改 `spec/`
- **不得**寫入 `workflow-b-pipeline/`
- 僅輸出 `workflow-a-direct/rtl/`

## 讀取範圍（Pass@1 公平性）

**僅允許**讀取 `spec/` 內 SPEC（見上方順序）。

**禁止**讀取或引用（含 `@`、搜尋、推測）：

- 任何 `RTLLM/` 路徑
- `verified_*.v`、黃金參考 RTL
- `testbench.v`（評測留給生成後人工／腳本）
- `_chatgpt35/`、`_chatgpt4/` 等既有生成碼

若 SPEC 缺 port／模組名，在回報中列出缺失並停止；**勿**從 testbench 或參考 RTL 補齊。

## Verilog

- 可綜合子集；單一 `clk`；預設 `rst_n` active-low 同步釋放
- `module_name.v` = 模組名
- port 與 SPEC 一致；註解標 SPEC 章節
- 組合邏輯 `always @(*)`，避免 latch

## 完成

- [ ] 所有對外 port
- [ ] 無多驅動 / 非預期組合迴路
- [ ] 列出 `[ASSUMPTION]`

回報：SPEC 路徑、`.v` 列表、假設。
