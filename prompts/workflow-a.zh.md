請依 **@workflow-a.zh** 規則執行 **Workflow A（直接 SPEC → RTL）**：

1. 讀取 `spec/` 原始 SPEC（優先 `design.spec.md`，其次 `design.spec.txt`）
2. 不要修改 `spec/`
3. 產生 Verilog 到 `workflow-a-direct/rtl/`
4. 完成後簡述模組列表、SPEC 對應、合理假設
