請依 **@rtl-pipeline-workflow-b**（**SKILL.zh.md**）只執行 **Workflow B — Agent1（SPEC 精煉）**。

讀取 `spec/` 原始 SPEC，覆寫 `workflow-b-pipeline/spec_refined.md`。不要產生 RTL 或完整 timing_plan。

**Reset**：port 名稱（`rst` / `rst_n`）與極性必須**依原始 SPEC 原文**，禁止擅自改成專案慣例的 `rst_n`。
