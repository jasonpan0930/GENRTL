請依 **@rtl-pipeline-workflow-b**（**SKILL.zh.md**）只執行 **Workflow B — Agent2（時序規劃）**。

讀取 `spec_refined.md`，覆寫 `timing_plan.md`（格式見 `reference.zh.md`）。若與 SPEC 矛盾，更新 `collaboration_log.md`；必要時修正 `spec_refined.md` 並記錄理由。不要產生 RTL。
